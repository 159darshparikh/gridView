import 'package:flutter/material.dart';

import '/container/create/create_grid_view_container.dart';

class CreateGridView extends StatelessWidget {
  const CreateGridView({super.key});

  @override
  Widget build(BuildContext context) {
    return const CreateGridViewContainer();
  }
}
